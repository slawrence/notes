MySQL Notes
=================

## Logging in

* `mysql -u root -p`

## Browsing

* `show databases`
* `use <SomeDbName>`
* `show tables`

## Dump file

* `mysql -u root -p <DB_NAME> < dump_file_name.sql`

